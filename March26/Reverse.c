//Incompleted reverse program.
#include<stdio.h>
#include<string.h>
int main(){
    printf("Enter your name : \n");
    char name[10];
    scanf("%s",name);
    int len = strlen(string ,name);
    printf("%d",len);
    // for(int i =5; i >= 0; i--){
    //     printf("%c", name[i]);
    // }

    return 0;
}