#include<stdio.h>
int main(){
    int value = 10;
    printf("%i \n",value); //For interger we can use both "%i" and "%d" format specifier. Both will generate the output.
    printf("%d",value);

    return 0;
}